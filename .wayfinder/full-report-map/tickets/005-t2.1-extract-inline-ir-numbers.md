<!-- label: wayfinder:task -->
<!-- status: open -->
<!-- triage: ready-for-agent -->
<!-- branch: single-feature-branch -->
# 005: T2.1 Extract Inline IR Numbers from PCE Testsheet

## Parent

[Full Report Generation Workflow Map](../map.md)

## Branch Strategy

Implemented directly on the single unified Full Report feature branch (no per-ticket branches).

## What to build

Enhancement to `TestsheetExtractor` and `src/testsheet/models.py` to parse inline IR photo numbers as typed integers (`int` / `tuple[int, ...]`) from `PCE Testsheet.xlsx` (SWG panels Col O, SWG Overview R26 Col O, SWG Overview Secondary R28 Col O/J, TX Col J, FP Col S, Battery Col H).

## Acceptance criteria

- [ ] `SwitchgearPanelSpec`, `SwitchgearSpec`, `TransformerSpec`, `LVDBSpec`, `BatteryBankSpec` extended with `photo_numbers: tuple[int, ...] = ()`.
- [ ] SWG panel IR numbers parsed from Column O (Rows 10, 14, 18, 22).
- [ ] SWG Overview IR numbers parsed from Row 26 Col O and Row 28 Col O/J.
- [ ] Transformer IR numbers parsed from Column J (Rows 33–37).
- [ ] Feeder Pillar IR numbers parsed from Column S (Rows 49, 53).
- [ ] Battery Bank IR numbers parsed from Row 59 Col H.
- [ ] Comma-separated strings (e.g. `"9,10"`) parsed cleanly via `re.findall`.
- [ ] Unit tests verifying parsing against mock workbooks without breaking existing tests.

## Blocked by

None (can start immediately)
