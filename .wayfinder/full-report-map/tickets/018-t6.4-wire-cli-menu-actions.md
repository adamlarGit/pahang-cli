<!-- label: wayfinder:task -->
<!-- status: open -->
<!-- triage: ready-for-agent -->
<!-- branch: single-feature-branch -->
<!-- issue: 37 -->
# 018: T6.4 Wire CLI Menu & Workflow Actions

## Parent

[Full Report Generation Workflow Map](../map.md)

## GitHub Issue

[#37](https://github.com/adamlarGit/pahang-cli/issues/37)

## Branch Strategy

Implemented directly on the single unified Full Report feature branch (no per-ticket branches).

## What to build

Registration of `FullReportAction` and `FullReportPostProcessingAction` in `src/cli_menu.py` and `src/project_workflow_actions.py` providing interactive station selection, dry-run telemetry, and execution summary tables.

## Reuse Pointers

- `src/cli_menu.py`: Re-use CLI menu registration structure and numbering.
- `src/project_workflow_actions.py`: Re-use interactive checkbox prompts, dry-run telemetry formatting, and execution summary table printers.
- `src/cli_selectors.py`: Re-use `select_substations_interactive()` with pre-checked selection per D44.
- `src/workflows/full_report.py`: Re-use `FullReportWorkflow`.
- `src/workflows/full_report_postprocessing.py`: Re-use `FullReportPostProcessingWorkflow`.

## Allowed Scaffolding

- Workflow action handlers `generate_full_reports_action` and `postprocess_full_reports_action` in `src/project_workflow_actions.py`.
- Menu items `"Generate Full Reports"` and `"Post-Process Full Reports (PDF + Testsheet Merge)"` wired at the bottom of the CLI menu in `src/cli_menu.py` per D44.
- Unit test suite `tests/test_full_report_cli_actions.py` mocking user input prompts and verifying action dispatch and summary formatting.

## Acceptance criteria

- [ ] Adds `"Generate Full Reports"` and `"Post-Process Full Reports (PDF + Testsheet Merge)"` to CLI menu.
- [ ] Presents interactive multi-station checkbox prompt with all ready stations pre-checked.
- [ ] Displays dry-run telemetry and pre-flight validation status before generation confirmation.
- [ ] Prints formatted execution summary box with counts for total, succeeded, failed, and output paths.
- [ ] Unit tests verifying CLI action execution and prompt routing.

## Blocked by

- [017: T6.3 Post-Processing PDF Conversion & Testsheet Append](017-t6.3-postprocessing-pdf-testsheet-merge.md)
